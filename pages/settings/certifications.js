import React from 'react';
import Layout from '../../components/Shared/Layout';
import Certifications from '../../components/Settings/Certifications';

function UserSettings() {
    return (
        <Layout>
            <Certifications />
        </Layout>
    );
}

export default UserSettings;
