import React from 'react';
import Layout from '../../components/Shared/Layout';
import TrainingSessions from '../../components/Settings/TrainingSessions';

function UserSettings() {
    return (
        <Layout>
            <TrainingSessions />
        </Layout>
    );
}

export default UserSettings;
